Will need a setup.sh script which
- configures any needed packages (python, pip, django, etc..)
- checks out an installs needed tools (clang-extra, afl, etc..)

Again, check the regression finding side project for reusable code.
